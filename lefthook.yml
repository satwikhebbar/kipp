pre-commit:
  parallel: true
  commands:
    typecheck:
      run: pnpm typecheck
    lint:
      run: pnpm lint
    docs:
      run: pnpm docs
    test:
      run: pnpm test
    test-integration:
      run: pnpm test:integration
    test-migration:
      run: pnpm test:migration
